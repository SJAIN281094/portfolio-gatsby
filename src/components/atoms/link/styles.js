import red from '@material-ui/core/colors/red';

const styles = (theme) => ({
  link: {
    color: theme.palette.common.white,
    textDecoration: 'none',
    fontWeight: 300,
  },
});

export default styles;
