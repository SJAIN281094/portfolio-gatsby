const styles = (theme) => ({
  navlinkWrapper: {
    display: 'flex',
    justifyContent: 'flex-end',
    listStyle: 'none',
  },
  navlink: {
    marginRight: '1.2rem',
    fontSize: '1.1rem',
  },
});

export default styles;
