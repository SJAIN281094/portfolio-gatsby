const styles = (theme) => ({
  headerWrapper: {
    position: 'fixed',
    background: 'rgba(255, 255, 255, 0.2)',
    backdropFilter: 'blur(3px)',
    top: 0,
  },
  headerContainer: {
    alignItems: 'center',
  },
  logo: {
    width: '5.5rem',
  },
});

export default styles;
